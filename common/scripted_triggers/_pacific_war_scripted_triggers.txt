pw_is_ally_with_sov = {
    controller = {
        OR = {
            is_in_faction_with = SOV
            is_ally_with = SOV
            tag = SOV
        }
    }
}
pw_is_ally_with_jap = {
    controller = {
        OR = {
            is_in_faction_with = JAP
            is_ally_with = JAP
            tag = JAP
        }
    }
}
pw_is_ally_with_usa = {
    controller = {
        OR = {
            is_in_faction_with = USA
            is_ally_with = USA
            tag = USA
        }
    }
}
